/*
resource "aws_s3_bucket_cors_configuration" "{{ bucket_name }}-bucket-cors-configuration" {
  bucket = aws_s3_bucket.{{ bucket_name }}.id
  cors_rule {
    allowed_headers = ["*"]
    allowed_methods = ["GET"]
    allowed_origins = ["*"]
    max_age_seconds = 3600
  }
}
*/
