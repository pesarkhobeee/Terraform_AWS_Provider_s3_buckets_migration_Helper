/*
resource "aws_s3_bucket_server_side_encryption_configuration" "{{ bucket_name }}-bucket-encryption-config" {
  bucket = aws_s3_bucket.{{ bucket_name }}.id

  rule {
    apply_server_side_encryption_by_default {
      sse_algorithm = "aws:kms"
    }
  }
}
*/
